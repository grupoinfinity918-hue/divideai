import {useEffect,useState} from 'react';
import Header from '../components/common/Header';
export default function Ranking(){const[d,setD]=useState([]);useEffect(()=>{fetch('/api/ranking/sellers').then(r=>r.json()).then(setD)},[]);return <><Header/><main className="max-w-5xl mx-auto px-4 py-8"><h1 className="text-2xl font-black">Ranking de vendedores</h1><p className="text-sm text-gray-500 mt-1">A posição é definida pelo valor total das vendas pagas.</p><div className="mt-6 grid gap-3">{d.map((s,i)=><div className="card p-4 flex items-center gap-4" key={s.id}><div className="w-10 h-10 rounded-full bg-pink-soft text-pink-neon font-black flex items-center justify-center">{i+1}</div>{s.avatarUrl?<img src={s.avatarUrl} className="w-12 h-12 rounded-full object-cover"/>:<div className="w-12 h-12 rounded-full bg-gray-100 flex items-center justify-center font-bold">{s.name[0]}</div>}<div className="flex-1"><p className="font-bold">{s.storeName||s.name}</p><p className="text-xs text-gray-500">{s.salesCount} vendas · ⭐ {s.ratingAverage.toFixed(1)} · {s.ratingCount} avaliações</p></div><div className="text-right"><p className="font-black text-pink-neon">R$ {s.totalSales.toFixed(2)}</p><p className="text-[10px] text-gray-400">em vendas</p></div></div>)}</div></main></>}