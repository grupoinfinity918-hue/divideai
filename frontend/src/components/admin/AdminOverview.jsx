import {useEffect,useState} from 'react'; import {authHeader} from '../../context/AuthContext';
export default function AdminOverview(){const[d,setD]=useState(null);useEffect(()=>{fetch('/api/admin/dashboard',{headers:authHeader()}).then(r=>r.json()).then(setD)},[]);if(!d)return <div className="card p-6">Carregando indicadores...</div>;const cards=[['Faturamento',`R$ ${d.gross.toFixed(2)}`],['Comissões',`R$ ${d.commissions.toFixed(2)}`],['Líquido vendedores',`R$ ${d.net.toFixed(2)}`],['Vendas',d.sales],['Pendentes',d.pending],['Clientes',d.users]];return <div><div className="grid grid-cols-2 md:grid-cols-3 gap-4">{cards.map(c=><div className="card p-5" key={c[0]}><p className="text-xs text-gray-500">{c[0]}</p><p className="text-2xl font-black mt-1 text-gray-900">{c[1]}</p></div>)}</div><div className="card p-5 mt-5"><h3 className="font-bold">Vendas por dia</h3><div className="mt-4 space-y-2">{Object.entries(d.byDay).slice(-14).map(([day,v])=><div key={day} className="flex items-center gap-3 text-xs"><span className="w-24 text-gray-500">{day}</span><div className="h-3 rounded-full bg-pink-neon/80" style={{width:`${Math.max(4,Math.min(100,(v/Math.max(...Object.values(d.byDay),1))*100))}%`}}/><b>R$ {v.toFixed(2)}</b></div>)}</div></div></div>}
